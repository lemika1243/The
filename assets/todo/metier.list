admin/configMois:
    .Metier:
        .[X] getMois()
        .[X] insertRegeneration(mois[]) -> insert into the Regeneration table
        .[X] deleteAllRegeneation()

admin/configCueilleure:
    .Metier:
        .[X] getCueilleure() -> returns all the Cueilleures
        .[X] updateCueilleure(idCueilleur, poidsMinimal, salaire) -> update where idCueilleur=idCueilleur

user/listPayement:
    .Metier:
        .[X] getNbj(datemin,datemax)
        .[X] getPoidsNormal(datemin,datemax) -> getNbj()*poidsMin par Cueilleures;
        .[X] getMontantCueilleureWithBonus(datemin,datemax)->return all the Cuilleure with bonuses
        by Cueilleures

user/accueil:
    .Metier:
        .[] getPrixMontantVente(datemin, datemax) -> getRendement()*prixVente par the
        .[] getTotalDepense(datemin,datemax) 