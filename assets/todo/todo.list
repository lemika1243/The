.login:
    .Affichage:
        .[X] Formulaire : email(email), mot de passe(password), Type d'utilisateur(select),Se connecter(submit)

.admin/acceuil:
    .Affichage:
        .[X] Liens : insertion the, insertion parcelle, insertion cueilleure, insertion categorie depense,
                renouveler salaire

.insertThe:
    .Affichage:
        .[X] Formulaire : Variete(text), ocupation(number), rendement(number), prix d'achat(number), prix de vente(number),
            valider(submit)
    
.insertParcelle:
    .Affichage:
        .[X] Formulaire : nom(text) , surface(number), the(select),valider(submit)

.insertCueilleure:
    .Affichage:
        .[X] Formulaire : nom(text), dateEmbauche(date),valider(submit)

.insertCategorieDepense:
    .[X] Formulaire : depense(text), valider(submit)


.updateSalaire:
    .[X] Formulaire : salaire(number), valider(submit)



.user/acceuil:
    .[X] Poids total cueillette
    .[X] Poids restant sur les parcelles
    .[X] Cout de revient/kg

.insertionCueillette:
    .Affichage:
        .[X] Formulaire : parcelle(select), cueilleure(select), poids(number), valider(submit)

.insertionDepense:
    .Affichage:
        .[X] Formulaire : categorie depense(select), date depense(date), montant(number), valider(submit)